import { spacing } from '../theme';

/** Height from screen bottom to preview card bottom edge (above map bottom chrome). */
export const MAP_PROFILE_PREVIEW_BOTTOM_OFFSET = 156;

/** Offset from safe top to search stack (below close / meta row). */
export const MAP_SEARCH_STACK_TOP_OFFSET = spacing.sm + 52;

/** Default offset from safe top to zoom +/- stack (below search row). */
export const MAP_ZOOM_CONTROLS_TOP_OFFSET = spacing.sm + 152;

/** Extra drop when place suggestions list is open. */
export const MAP_ZOOM_SUGGESTIONS_EXTRA_TOP = 108;
