# Cloud Agents

On every new run, read **`docs/CLOUD_AGENT_HANDOFF.md`** first (canonical repo state, abandoned threads, open PRs).

# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.
