/** @deprecated Import from `./legal` — re-exported for backward compatibility. */
export {
  getLegalDocument,
  getLegalDocumentLinks,
  legalDocumentLinks,
  LEGAL_CONTACT_EMAIL,
  LEGAL_PRIVACY_EMAIL,
  type LegalDocument,
  type LegalDocumentId,
  type LegalDocumentLink,
  type LegalDocumentSection,
} from './legal';
